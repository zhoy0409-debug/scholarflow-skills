# 内容单元去重与版本规则

## 去重类型

第一期只允许 4 类：

- `完全重复`
- `同义重复`
- `近似重复`
- `重复讲述`

## 处理规则

- `完全重复`：默认合并
- `同义重复`：默认合并
- `近似重复`：不合并，保留独立单元
- `重复讲述`：视叙述角度决定是否并入主单元

## 主单元规则

需要合并时，必须指定 `canonical: true` 的主单元作为当前有效对象。

## 版本规则

只有语义边界、适用范围、核心结论或关键步骤发生变化，才提升 `version`。
